<?xml version="1.0"?>
<!--
    Description: Hierarchy 1 (single grouping)
    Eval: result['bozo'] == 0
    Eval: len(result['feeds'][0]['tags']) == 1
    Eval: result['feeds'][0]['tags'] == ['grouping']
    Eval: len(result['feeds'][0]['categories']) == 1
    Eval: result['feeds'][0]['categories'][0] == ['grouping']
-->
<opml version="2.0">
    <head></head>
    <body>
        <outline text="grouping">
            <outline text="feedname" type="rss" xmlUrl="http://domain/feed" />
        </outline>
    </body>
</opml>
